package cloudsearch.scalatra.routes

import cloudsearch.scalatra.JsonApi

/**
 * Created by herval on 7/25/15.
 */
class SearchRoutes extends JsonApi {

//  post("/multi") {
//    val req = parsedBody.extract[MultiSearchRequest]
//    val accounts = // TODO fetch user accounts based on auth token
//    async {
//      val searches = new MultiSearch(accounts).all(req)
//      Future.sequence(searches).map { serviceSpecific =>
//        serviceSpecific.flatMap { results =>
//          results.results
//        }
//      }
//    }
//  }

}
